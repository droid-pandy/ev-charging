# Streamlit secrets configuration (OPTIONAL)
# This file is optional - the app works with defaults if not present
# Copy this to secrets.toml and modify if needed

# Agent service URL (default: http://localhost:8080/invocations)
AGENT_URL = "http://localhost:8080/invocations"

# JWT token for agent authentication (default: dummy-token)
JWT_TOKEN = "dummy-token"
